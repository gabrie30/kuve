---
# Can find pods by namespaces or labels. If labels is set, POD_LABEL_KEY must also be set
FIND_PODS_BY: "labels"

# This is the key used while searching by labels eg kubectl get pods --all-namespaces -l=POD_LABEL_KEY=some_app
POD_LABEL_KEY: "app"
